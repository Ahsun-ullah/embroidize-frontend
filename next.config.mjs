const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: '',
        protocol: 'https',
        port: '',
        pathname: '/**',
      },
    ],
  },
};

export default nextConfig;
